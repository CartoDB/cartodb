describe("Pattern picker", function() {
  var view;

  beforeEach(function() {
    view = new cdb.admin.PatternPicker({
      model: new cdb.core.Model()
    });
  });

  afterEach(function() {});

  // it("should render properly", function() {})
  // it("should apply the color chosen previously", function() { })
  // it("should apply the image pattern chosen previously", function() { })
  // it("should let you click over any suggested colors", function() { })
  // it("should let you change the color in the input typing it", function() { })
  // it("shouldn't let you type in the input if a pattern image is selected", function() { })
});