
## GMapsBaseLayerView -> Default Map Types

map.setMapTypeId

## GMapsCartoDBLayerGroupView -> Custom MapType object

Must implement:

- getTile

map.overlayMapTypes.setAt(index, overlay);
map.overlayMapTypes.getArray().indexOf(overlay)
map.overlayMapTypes.removeAt(index);

## GMapsTorqueLayerView -> google.maps.OverlayView 

Must implement:

- onAdd
- draw
- onRemove

Must invoke:

- setMap(map) / setMap(null)