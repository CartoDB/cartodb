language: node_js
node_js:
  - "6.9.2"

before_install:
  - git submodule update --init --recursive

install:
  - yarn install

script:
  - grunt test

notifications:
  email:
    on_success: never
    on_failure: change

cache:
  yarn: true
  directories:
    - node_modules