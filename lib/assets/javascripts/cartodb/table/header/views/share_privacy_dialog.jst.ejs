<p>This visualization contains private tables. In order to make this public you need to do the following tables public too.</p>
<ul>
  <% _.each(related_tables, function(table) { %>
    <% if (table.privacy && table.privacy.toLowerCase() == "private") { %>
      <li>
        <span class="loader left">loader</span>
        <a href="/tables/<%= table.id %>" class="name"><%= table.name %></a>
        <span class="status <%= table.privacy.toLowerCase() %>"><%= table.privacy.toUpperCase() %></span>
        <a href="#/make-public" data-id="<%= table.id %>" class="right button grey smaller make_public">make public</a>
      </li>
    <% } %>
  <% }) %>
</ul>
<p class="error hide">Something went wrong, please try it again.</p>