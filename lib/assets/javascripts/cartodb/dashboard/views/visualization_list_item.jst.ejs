<div class="content">
  <h3><a href="/visualizations/<%= id %>"><%= name  %></a><a href="#delete" class="delete" original-title="Delete"></a></h3>
  <p><% print(description || '') %></p>
  <div class="tags">
    <% if (tags) { %>
      <% for (var tag in tags) { %>
        <% if (tags[tag] != "") { %>
          <a href="#/tag/<%= tags[tag] %>/1"><%= tags[tag] %></a>
        <% } %>
      <% } %>
    <% } %>
  </div>
  <div class="footer">
    <div class="graph"></div>
    <div class="colors"></div>
  </div>
</div>