<% content_for :head do %>
  <%= stylesheet_link_tag('reset', 'front/home') %>
<% end %>
<section>
  <h1>We love <strong>open-data*</strong><span>*With some <%= link_to 'limitations', limits_path %></span></h1>
  <p>CartoDB is a new geospatial database on the cloud that allows you to manage your data and create engaging visualizations with it</p>
  <%= form_for @user do |f| %>
    <span><p>Join the beta</p><a href="<%= login_path %>">already registered?</a></span>
    <span>
      <input id="email" name="email" type="text" />
      <input name="commit" type="submit" value="Sign up" />
    </span>
    <% if @user.errors.any? %>
    <div class="error_content">
      <p><span><%= @user.errors.full_messages.join(',').capitalize %></span></p>
    </div>
    <% end %>
  <% end %>
  <p class="follow">or <a href="http://www.twitter.com/cartodb" class="twitter" target="_blank" rel="nofollow">follow us on twitter</a></p>
</section>
<footer>CartoDB is a product of <a href="http://www.vizzuality.com" target="_blank" rel="nofollow">vizzuality</a></footer>
