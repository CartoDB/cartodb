<% content_for :title do %><%= t('signup.already.enabled') %><% end %>

<div class="Sessions CDB-Text">
  <div class="Sessions-content">
    <div class="Sessions-inner">
      <div class="u-inner">
        <div class="Sessions-center Sessions--centered">
          <div class="Sessions-logoContainer">
            <picture class="Navbar-brand">
              <%= image_tag("layout/carto-logo-darkblue.svg", :alt => t('carto_name')) %>
            </picture>
          </div>

          <div class="js-info">
            <h1 class="Sessions-title u-tspace-m"><%= t('signup.already.active') %></h1>
            <p class="Sessions-description"><a href="/login"><%= t('signup.already.login') %></a><%= t('signup.already.enjoy') %></p>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
