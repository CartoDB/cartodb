<div class="u-inner">
  <div class="SupportBanner-info">
    <h4 class="SupportBanner-infoTitle">
      <% if (userType === 'org' || userType === 'client') { %>
        As a paying customer you have access to our dedicated support.
      <% } else if (userType === "internal") { %>
        You are part of CartoDB, you deserve an outstanding support. 
      <% } else { %>
        For all technical questions contact our community support forum.
      <% } %>
    </h4>
    <p class="SupportBanner-infoDescription">
      <% if (userType === 'org' || userType === 'client') { %>
        Remember that there is a lot of information in our <a href="http://gis.stackexchange.com/questions/tagged/cartodb" class="SupportBanner-infoDescriptionLink">community forums</a>.
      <% } else if (userType === "internal") { %>
        Don't forget to share your knowledge in our <a href="http://gis.stackexchange.com/questions/tagged/cartodb" class="SupportBanner-infoDescriptionLink">community forums</a>.
      <% } else { %>
        If you experience any problem with the CartoDB service, feel free to <a class="SupportBanner-infoDescriptionLink" href="mailto:support@cartodb.com">contact us</a>.
      <% } %>
    </p>
  </div>
  <div class="SupportBanner-link">
    <% if (userType === 'org') { %>
      <a href="mailto:enterprise-support@cartodb.com" class="Button Button--overBlack">
        <span>Enterprise support</span>
      </a>
    <% } else if (userType === 'client' || userType === 'internal') { %>
      <a href="mailto:support@cartodb.com" class="Button Button--overBlack">
        <span>Contact support</span>
      </a>
    <% } else { %>
      <a href="http://gis.stackexchange.com/questions/tagged/cartodb" class="Button Button--overBlack">
        <span>Community support</span>
      </a>
    <% } %>
  </div>
</div>