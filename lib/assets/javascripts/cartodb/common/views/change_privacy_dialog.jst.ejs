<div class="mamufas">
  <section class="block modal <%= modal_type %>">

    <% if (limitation) { %>
    <div class="warning" style="display: block;">Privacy and visibility options are only available on paid accounts.</div>
    <% } %>

    <a href="#close" class="close">x</a>
    <div class="head">
      <h3><%= title %></h3>
      <% if (description) { %>
        <p class="margin5"><%= description %></p>
      <% } %>
    </div>
    <div class="content">
      <ul>
        <li class="option public">
          <a class="radiobutton" data-privacy="PUBLIC"><span class="radio"></span><span>Public on the web</span><i></i></a>
        </li>
        <li class="option link">
          <a class="radiobutton" data-privacy="LINK"><span class="radio"></span><span>Accessible with the link</span><i></i></a>
        </li>
        <li class="option private">
          <a class="radiobutton" data-privacy="PRIVATE"><span class="radio"></span><span>Private</span><i></i></a>
        </li>
      </ul>
    </div>
    <% if(include_footer) { %>
      <div class="foot">
        <a href="#go" class="right ok <%= ok_button_classes %>"><%= ok_title %></a>
      </div>
    <% } %>
  </div>
</div>
