<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset='utf-8' />
    <title><%= yield :page_title %>CartoDB</title>
    <link id="favicon" rel="shortcut icon" href="/img/favicon.ico" />
    <%= yield :css %>
    <%= yield :js %>
    <%= csrf_meta_tag %>
    <%= render 'shared/analytics' %>
    <noscript><meta http-equiv="refresh" content="0;url=/no_javascript.html"></noscript>
  </head>
  <body>
    <%= render 'admin/shared/header', :table => (true if controller_name == "tables" && action_name == "show" )  %>
    <%= yield %>
    <% if controller_name != "tables" && action_name != "show" %>
      <%= render 'admin/shared/footer' %>
    <% end %>
    <%#= yield :mamufas %>
  </body>
</html>
