name: Private CARTO hook
on: [pull_request]

jobs:
  hook:
    runs-on: ubuntu-latest
    steps:
      - name: Repository Dispatch
        uses: peter-evans/repository-dispatch@v1
        with:
          token: ${{ secrets.TEST_PAT }}
          repository: CartoDB/cartodb-private
          event-type: my-event