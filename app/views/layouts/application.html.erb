<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset='utf-8' />
    <title><%= yield :page_title %>CartoDB</title>
    <link id="favicon" rel="shortcut icon" href="/favicons/favicon.ico" />
    <%= yield :css %>
    <!--[if gt IE 8]><%= stylesheet_link_tag :fonts_ie %><![endif]-->
    <%= yield :js %>
    <%= csrf_meta_tag %>
    <%= render 'shared/analytics', ua: Cartodb.config[:google_analytics]["primary"] %>
    <noscript><meta http-equiv="refresh" content="0;url=/no_javascript.html"></noscript>
  </head>
  <body>
    <% if ( controller_name != "visualizations" || action_name != "show" ) %>
      <%= render 'admin/shared/header' %>
    <% else %>
      <%= render 'admin/shared/table_header' %>
    <% end %>
    <%= yield %>
    <% if controller_name != "visualizations" || action_name != "show" %>
      <%= render 'admin/shared/footer' %>
    <% end %>
  </body>
</html>
