<div id="<%= id %>">
  <% if (totalCount > 0) { %>
    <i class="iconFont iconFont-Lock ContentFooter-lockedIcon"></i>
    <a href="<%= url %>">Show your <%= totalCount %> locked <%= pluralizedContents %></a>
  <% } %>
</div>
