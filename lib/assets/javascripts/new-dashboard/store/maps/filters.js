const lockedParams = {
  'locked': true
};
const sharedParams = {
  'shared': 'only'
};
const favoritedParams = {
  'only_liked': true
};

export default {
  lockedParams,
  sharedParams,
  favoritedParams
};
